/*
Considerando o registro da questão anterior, elabore um programa para armazenar o
nome e nota de 35 alunos da disciplina Estruturas de Dados. O programa deverá
mostrar os registros dos 35 alunos e notas.
*/
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Definição do registro (struct) para o aluno
struct Aluno {
    char nome[50];
    float notas[3];
};

int main() {
    // Declaração de um vetor de 35 registros do tipo Aluno
    struct Aluno turma[2];
    int i;

    // --- Leitura dos dados de 35 alunos ---
    printf("--- Cadastro de Alunos ---\n\n");
    for (i = 0; i < 2; i++) {
        printf("Registro do Aluno %d:\n", i + 1);
        
        // Limpa o buffer do teclado antes de ler a string
        while (getchar() != '\n');
        printf("Nome: ");
        fgets(turma[i].nome, sizeof(turma[i].nome), stdin);
        turma[i].nome[strcspn(turma[i].nome, "\n")] = 0; // Remove o '\n'

        printf("Nota da AP1: ");
        scanf("%f", &turma[i].notas[0]);

        printf("Nota da AP2: ");
        scanf("%f", &turma[i].notas[1]);

        printf("Nota FINAL: ");
        scanf("%f", &turma[i].notas[2]);
        
        printf("\n");
    }

    // --- Exibição dos dados dos 35 alunos ---
    printf("\n--- Relatorio de Alunos e Notas ---\n");
    for (i = 0; i < 2; i++) {
        printf("\nAluno %d\n", i + 1);
        printf("Nome: %s\n", turma[i].nome);
        printf("Notas:\n");
        printf("  - AP1: %.2f\n", turma[i].notas[0]);
        printf("  - AP2: %.2f\n", turma[i].notas[1]);
        printf("  - FINAL: %.2f\n", turma[i].notas[2]);
    }

    return 0;
}
